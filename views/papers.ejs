<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Research Papers</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    /* Header */
    header {
      background-color: #0d47a1;
      color: #fff;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    header h1 {
      margin: 0;
      font-weight: 700;
      font-size: 1.8rem;
    }
    header nav a {
      color: #fff;
      text-decoration: none;
      margin-left: 1.5rem;
      font-weight: 500;
      transition: color 0.2s ease;
    }
    header nav a:hover {
      color: #ffd700;
    }
    body {
      background-color: #f8f9fb;
      font-family: 'Segoe UI', sans-serif;
    }
    h2 {
      color: #0d47a1;
      font-weight: 600;
    }
    .card {
      border: 1px solid #e0e6ed;
      border-radius: 12px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.06);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 6px 14px rgba(0,0,0,0.1);
    }
    .card-title {
      font-size: 1.1rem;
      font-weight: 600;
      color: #0d47a1;
    }
    .card-subtitle {
      font-size: 0.9rem;
      color: #495057;
    }
    .meta {
      font-size: 0.85rem;
      color: #343a40;
      margin-bottom: 0.3rem;
    }
    .badge {
      font-size: 0.75rem;
      background-color: #e3f2fd;
      color: #0d47a1;
      border-radius: 8px;
    }
    .btn-outline-primary {
      border-radius: 8px;
      padding: 0.4rem 0.9rem;
      font-size: 0.85rem;
      border-color: #118ab2;
      color: #118ab2;
    }
    .btn-outline-primary:hover {
      background-color: #118ab2;
      color: #fff;
    }
    .btn-outline-secondary {
      border-radius: 8px;
      padding: 0.4rem 0.9rem;
      font-size: 0.85rem;
      border-color: #6c757d;
      color: #6c757d;
    }
    .btn-outline-secondary:hover {
      background-color: #6c757d;
      color: #fff;
    }

    footer {
      background-color: #0d47a1;
      color: #fff;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
    }
    
  </style>
</head>
<body>
  <header>
    <h1>Research Paper Explorer</h1>
    <nav>
      <a href="/api/search">Home</a>
      <a href="/api/search/#about">About</a>
      <a href="/api/search/#contact">Contact</a>
    </nav>
  </header>

<div class="container mt-5">
  <h2 class="mb-4 text-center">Research Papers</h2>
  <div class="row">
    <div>
      
    </div>
    <% for(let p of papers){ %>
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card h-100">
          <div class="card-body d-flex flex-column">
            
            <h5 class="card-title"><%= p.title %></h5>
            
            <h6 class="card-subtitle mb-2">Authors</h6>
            <p class="mb-2">
              <% for(let a of p.authors){ %>
                <span class="badge me-1 mb-1"><%= a %></span>
              <% } %>
            </p>

            <p class="meta"><strong>Year:</strong> <%= p.year %></p>
            <p class="meta"><strong>Venue:</strong> <%= p.venue %></p>
            <p class="meta"><strong>Citations:</strong> <%= p.citation_count %></p>

            <p class="meta"><strong>DOI:</strong> 
              <% if(p.doi !== "N/A"){ %>
                <a href="<%= p.doi %>" target="_blank" style="color:#118ab2;"><%= p.doi %></a>
              <% } else { %> N/A <% } %>
            </p>

            <p class="meta"><strong>PDF:</strong> 
              <% if(p.pdf_url !== "N/A"){ %>
                <a href="<%= p.pdf_url %>" target="_blank" style="color:#118ab2;">Download</a>
              <% } else { %> N/A <% } %>
            </p>

            <div class="mt-auto d-flex justify-content-between">
              <a href="/api/papers/<%= encodeURIComponent(p.id.replace('https://openalex.org/', '')) %>" 
                 class="btn btn-outline-primary btn-sm">Details</a>
              <a href="<%= p.doi !== 'N/A' ? p.doi : '#' %>" 
                 target="_blank" class="btn btn-outline-secondary btn-sm">View DOI</a>
            </div>

          </div>
        </div>
      </div>
    <% } %>
  </div>
</div>

<footer>
    &copy; 2025 Research Explorer. All rights reserved.
  </footer>

</body>
</html>
